<div class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h2 class="m-0 text-dark">Purchases</h2>
      </div>
      <p id="notice"><%= notice %></p>
    </div>
  </div>
</div>
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-body">
            <span>
              <div class="container">
                <div class="row">
                  <%= link_to 'New Purchase', new_purchase_path, :class => "btn btn-primary" %>
                </div>
                <div class="row">
                  <div class="col-md-12 mt-4 text-center">
                    <table>
                      <thead>
                        <tr>
                          <th>User</th>
                          <th>Plan</th>
                          <th>Discount</th>
                          <th colspan="3"></th>
                        </tr>
                      </thead>

                      <tbody>
                        <% @purchases.each do |purchase| %>
                        <tr>
                          <td><%= purchase.user_id %></td>
                          <td><%= purchase.plan_id %></td>
                          <td><%= purchase.discount_id %></td>
                          <td><%= link_to 'Show', purchase, :class => "btn btn-primary" %></td>
                          <td><%= link_to 'Edit', edit_purchase_path(purchase), :class => "btn btn-primary" %></td>
                          <td>
                            <%= link_to 'Destroy', purchase, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-primary" %>
                          </td>
                        </tr>
                        <% end %>
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>